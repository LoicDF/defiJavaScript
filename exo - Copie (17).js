let tableau = [5,4,3,2,1];
// let element1 =[];
let element2 = [];
var taille = tableau.length;

for(i=0; i< taille; i++){
  element2.push(tableau.pop());
};
  // tableau.push(tableau.slice[4,0,tableau]);

document.write(element2);
